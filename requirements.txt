google-api-python-client>=2.137.0  # For googleapiclient.discovery (YouTube API access)
pyyaml>=6.0.1                      # For yaml (loading credentials from YAML files)
pandas>=2.2.0                      # For pandas (data manipulation with DataFrames)
yt-dlp>=2024.10.22                 # For yt_dlp (YouTube video metadata extraction)
youtube-transcript-api>=0.6.2      # For youtube_transcript_api (fetching transcripts)
langchain-community>=0.3.0         # For langchain_community (vector stores, document loaders)
langchain-openai>=0.2.0            # For langchain_openai (OpenAI embeddings and ChatOpenAI)
langchain-core>=0.3.0              # For langchain_core (runnables, parsers, prompts)
chromadb>=0.5.5                    # For Chroma vector store (used via langchain_community)
openai>=1.45.0                     # For OpenAI API integration (used by langchain_openai)
streamlit>=1.38.0                  # For streamlit (web app interface)